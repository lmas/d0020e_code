package main

/*
func Test_structupdate(t *testing.T) {

	asset := UnitAsset{
		Min_temp: 20.0,
	}
	// Simulate the input signal
	inputSignal := forms.SignalA_v1a{
		Value: 17.0,
	}
	// Call the setMin_temp function
	asset.setMin_temp(inputSignal)

	// Check if Min_temp is updated correctly
	if asset.Min_temp != 17.0 {
		t.Errorf("expected Min_temp to be 17.0, got %f", asset.Min_temp)
	}
}
*/
// #1 Test if struc can handle floats
// #2 Test if we trys to update the struct, that infact the value is updated correctly
// #3
